# Task2
internship task
# Port Scanner

## Overview
This Python program checks whether a specific port on a given host is open or closed using socket programming.

## Features
- Accepts hostname or IP address as input
- Accepts a port number
- Determines whether the port is open or closed
- Uses Python's socket module

## Usage
Run the program:

```bash
python port_scanner.py
